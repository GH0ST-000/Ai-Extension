openapi: '3.0.3'
info:
  title: Pets API YAML
  version: '1.0.0'
paths:
  /health:
    get:
      operationId: healthCheck
      summary: Health
      responses:
        '200':
          description: OK
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    default: true
